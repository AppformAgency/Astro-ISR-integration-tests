{
  "devDependencies": {
    "glob": "^10.2.2",
    "vitest": "^0.31.0",
    "zx": "^7.2.2"
  },
  "dependencies": {
    "@astro/isr": "git+https://github.com/AppformAgency/ISR-package",
    "astro": "^2.4.2"
  },
  "scripts": {
    "test": "vitest run --silent",
    "build": "astro --root ./app build"
  }
}
